/**
 * Mobile App Configuration
 * Replace the IP with your computer's local IP to test on a physical device.
 */
const CONFIG = {
    // For Android Emulator, use: http://10.0.2.2:5000/api
    // For Physical Device, use your computer's local IP (e.g. 192.168.x.x)
    API_URL: 'https://einsdreambcknd.vercel.app/api',
    GOOGLE_WEB_CLIENT_ID: 'your_google_auth_client_id.apps.googleusercontent.com',
};

export default CONFIG;
